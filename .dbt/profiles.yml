bengaluru_mobility_dbt:
  outputs:

    dev:
      type: sqlite
      threads: 1
      database: 'database'
      schema: 'main'
      schemas_and_paths:
        main: "{{ env_var('GITHUB_WORKSPACE') }}/data/processed/mobility.db"
      schema_directory: "{{ env_var('GITHUB_WORKSPACE') }}/data/processed"

  target: dev